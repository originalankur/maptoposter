---
name: repo-tech-stack
description: "Use when building, testing, configuring, or adding dependencies in repo. Covers: Python >=3.11; GeoPandas; OSMnx; Matplotlib. Commands: python create_map_poster.py, bash test/all_variations.sh, flake8."
---

<!-- generated-by: skillgen v0.0.0-dev | repo: repo | commit: 6d03069f34558494ccc7fc2a7de87df7a7543fd5 | date: 2026-08-06T07:56:23Z -->
# Tech stack

## Languages
- Python >=3.11

## Frameworks & runtimes
- **GeoPandas** — Geospatial data handling for map geometries
- **OSMnx** — Download and work with OpenStreetMap street networks
- **Matplotlib** — Rendering map posters and visual elements

## Key dependencies
- **Shapely** — Geometry operations for map features
- **NetworkX** — Graph representation of street networks
- **Pillow** — Image handling for final poster output
- **Pandas** — Tabular data handling for geospatial attributes
- **tqdm** — Progress bars for long-running map generation

## Package management
Python project managed via pyproject.toml (PEP 621) plus requirements.txt pinning exact versions; no evidence of poetry/pipenv; dependencies likely installed with pip using requirements.txt.

## Commands
| Purpose | Command |
|---|---|
| Run the main script to generate a map poster via command-line arguments. | `python create_map_poster.py` |
| Generate multiple poster variations for testing themes and layouts. | `bash test/all_variations.sh` |
| Run flake8 linting using configuration from .flake8. | `flake8` |

## Testing
No explicit Python unit test framework (like pytest) found. A shell script at test/all_variations.sh exercises the main script across many parameter combinations as an integration-style test harness. No conventional tests/ or test_*.py files detected.

## CI/CD & infrastructure
GitHub Actions workflows under .github/workflows (conflicts.yml, pr-checks.yml) configure CI for pull requests and conflict checks; exact commands are defined there but not fully detailed in this summary.

## Configuration & environment
Configuration is primarily via CLI arguments to create_map_poster.py and JSON theme files under themes/*.json. No .env or secrets manager integration found; .gitignore excludes typical virtualenv and cache directories. No explicit environment variable-based configuration documented.

## Working agreements
- Use pip with requirements.txt or a PEP 621-compatible tool to install dependencies respecting pinned versions.
- Before changing rendering logic, run bash test/all_variations.sh to visually inspect that generated posters look correct across themes.
- Honor the .flake8 configuration when adjusting code style and run flake8 before submitting changes.
- Preserve the structure and fields of JSON theme files under themes/ when adding or modifying themes.
- When adding new dependencies, update both pyproject.toml and requirements.txt to keep them in sync.

*Verified claims: 0/0.*

---
*Generated by skillgen. Regenerate with `skillgen scan <repo>`.*
